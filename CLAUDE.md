## Agent skills

### Issue tracker

Issues 追蹤於 GitHub Issues（`haogroot/receipt-system`），使用 `gh` CLI 操作。詳見 `docs/agents/issue-tracker.md`。

### Domain docs

Single-context 專案結構：`CONTEXT.md` + `docs/adr/` 位於專案根目錄。詳見 `docs/agents/domain.md`。
